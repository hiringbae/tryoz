# GitHub Copilot Coding Agent

```bash
npx tryoz setup --copilot-agent --project
```

GitHub Copilot Coding Agent setup is project-oriented. It writes repository MCP
configuration and an Oz policy block so the coding agent can use Oz for external
library and API documentation.
