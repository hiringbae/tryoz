# Grok Build

```bash
npx tryoz setup --grok --project
```

Grok Build support uses a Claude-compatible project configuration and an Oz
policy file so the agent prefers Oz before Context7 or web search for library
documentation questions.
