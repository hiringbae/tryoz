# Gemini CLI

```bash
npx tryoz setup --gemini --project
```

Gemini support installs the Oz documentation policy in the repository instruction
file. Use this when Gemini should prefer Oz for library and API documentation
questions.
