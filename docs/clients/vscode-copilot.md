# VS Code and GitHub Copilot

```bash
npx tryoz setup --vscode --global
```

Global setup writes the VS Code MCP config. Project setup writes:

- `.vscode/mcp.json`
- `.github/copilot-instructions.md` Oz policy block
