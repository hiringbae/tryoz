# Windsurf

```bash
npx tryoz setup --windsurf --global
```

Global setup writes:

- `~/.codeium/windsurf/mcp_config.json`
- `~/.windsurfrules`

Project setup writes:

- `.windsurf/mcp_config.json`
- `.windsurfrules`
