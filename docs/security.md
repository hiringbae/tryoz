# Security

Do not open public issues for secrets, token leaks, authentication bypasses, or
hosted service vulnerabilities.

Report security issues privately using the contact path in `SECURITY.md`.

The CLI writes selected agent config files and marked Oz policy blocks. It does
not intentionally collect file contents, prompts, API keys, or absolute project
paths.
